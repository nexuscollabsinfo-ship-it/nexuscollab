// Relations for the NEXUS COLLABS schema
// Using simple foreign key references in queries rather than explicit Drizzle relations
// to keep the schema flexible and avoid circular dependency issues.

export {};
